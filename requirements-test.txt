# Lightweight dependencies needed by the repository's regression scripts.
# The CV stack is stubbed by scripts/_test_support.py, keeping CI fast. Pin the
# same versions you develop against: a stubbed request body that satisfied
# aiohttp 3.13 failed on the 3.14 pinned here, and only CI saw it.
aiohttp==3.14.3
beautifulsoup4==4.12.3
discord.py==2.4.0
gspread==6.1.3
numpy==1.26.4
Pillow==12.3.0
pyflakes==3.4.0
python-dotenv==1.2.3
rapidfuzz==3.10.1
requests==2.34.2
tzdata==2025.2
