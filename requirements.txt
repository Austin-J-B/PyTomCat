#cu126 is the oldest CUDA index that ships torch 2.8.0 wheels; any NVIDIA
#driver new enough for CUDA 12.1 also runs 12.6 builds (CUDA 12.x minor
#version compatibility).
--extra-index-url https://download.pytorch.org/whl/cu126

#Core Discord bot stack
discord.py==2.4.0
python-dotenv==1.2.2
aiohttp==3.13.5

#Google Sheets + OAuth
gspread==6.1.3
google-api-python-client==2.146.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
httplib2==0.22.0

#Data processing / parsing
numpy==1.26.4
Pillow==12.2.0
rapidfuzz==3.10.1
beautifulsoup4==4.12.3

#Computer vision helpers
opencv-python-headless==4.10.0.84
ultralytics>=8.3.200

#PyTorch (+cu126 for Windows/Nvidia). torchvision must match torch's minor
#version: 0.23.x pairs with torch 2.8.x (the old 0.19.1 pin paired with
#torch 2.4 and made this file unresolvable next to torch==2.8.0).
torch==2.8.0
torchvision==0.23.0

#HTTP utilities
requests==2.34.2

#Modal cloud (CV inference offload)
modal==1.4.2
