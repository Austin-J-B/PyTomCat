--extra-index-url https://download.pytorch.org/whl/cu128

#Core Discord bot stack
discord.py==2.4.0
python-dotenv==1.2.3
aiohttp==3.14.3

#Google Sheets + OAuth
gspread==6.1.3
google-api-python-client==2.146.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
httplib2==0.32.0

#Data processing / parsing
numpy==1.26.4
Pillow==12.3.0
rapidfuzz==3.10.1
beautifulsoup4==4.12.3

#Computer vision helpers
opencv-python-headless==4.10.0.84
ultralytics>=8.3.200

#PyTorch (+cu128 for Windows/Nvidia). Matches the 2.8.0 that
#requirements-droplet.txt and the Modal image already pin, so the gallery
#tensors written here load unchanged everywhere else.
torch==2.14.0+cpu
torchvision==0.23.0+cu128

#HTTP utilities
requests==2.34.2

#Modal cloud (CV inference offload)
modal==1.4.2
